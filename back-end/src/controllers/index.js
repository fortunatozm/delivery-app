const authController = require('./auth.controller');
const productsController = require('./products.controllers');
const checkoutController = require('./checkout.controllers');

module.exports = {
  authController,
  productsController,
  checkoutController,
};
